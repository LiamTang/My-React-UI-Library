/** @format */

import React from "react";
import TitleBar from ".";

export default {
	title: "UI Component /TitleBar",
	component: TitleBar,
};

export const Default = () => <TitleBar />;
