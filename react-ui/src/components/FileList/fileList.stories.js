/** @format */

import React from "react";
import FileList from ".";

export default {
	title: "Page Component /FileList",
	component: FileList,
};

export const Default = () => <FileList />;
