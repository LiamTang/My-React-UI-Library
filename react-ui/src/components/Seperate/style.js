/** @format */

import styled from "styled-components";

const StyledSeperate = styled.div`
	width: 100%;
	height: 1px;
	border-bottom: 1px solid ${({ theme }) => theme.grey4};
`;

export default StyledSeperate;
